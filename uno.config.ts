import { defineConfig } from 'unocss'

export default defineConfig({
  theme: {
    fontFamily: {
      sans: 'Inter',
    },
    colors: {
      primary_dark: '#212121',
      secondary_light: '#f3f3f3',
    }
  }
})
